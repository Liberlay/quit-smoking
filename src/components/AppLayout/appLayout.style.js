import { StyleSheet } from 'react-native'

export default styles = StyleSheet.create({
  background: {
    width: '100%',
    height: '100%',
  },

  container: {
    alignItems: 'center',
    height: '100%',
    overflow: 'hidden',
  },
})
